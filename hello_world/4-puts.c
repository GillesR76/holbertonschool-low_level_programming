#include <stdio.h>
int main(void)
	/**
	 * Using the main function as the entry point of the program i.e where the 
	 * execution of the program begins
	 */
{
	/**
	 * The code below will create a string and print it using puts
	 */

	char string1[] = "\"Programming is like building a multilingual puzzle";

	puts(string1);

	return (0);
}
